# 03 UI

At this checkpoint:

- The ArcGIS vector basemap is displayed.
- Historic places appear on the map.
- The same places appear as HTML cards in the sidebar.
- Each card displays information from the GeoJSON.
- The number of sidebar cards matches the number of mapped places.
- Clicking a map feature still opens its popup.

The map and sidebar do not interact with each other yet.

If the map and sidebar show the same dataset, continue to selection.
